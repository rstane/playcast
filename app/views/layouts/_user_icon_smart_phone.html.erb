<% if user.present? %>
  <%= link_to_unless( user.nickname.blank?, image_tag( user.image, size: "28x28", class: "margin_minus2" ), user_path(user), rel: "tooltip", "data-original-title" => user.nickname, title: user.nickname ) %>
<% end %>
