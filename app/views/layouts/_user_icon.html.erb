<% if user.present? %>
  <%= link_to_unless( user.nickname.blank?, image_tag( user.image, size: "25x25", class: "margin_minus2" ), (Settings.provider_page + user.nickname), rel: "tooltip", "data-original-title" => user.nickname, title: user.nickname, target: "_blank" ) %>
<% end %>
